#!/usr/bin/env ruby
require_relative '../lib/turn.rb'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Welcome to Tic Tac Toe!"
display_board(board)

turn(board) #ask for input

user_input = gets.strip.to_i #get input & #convert input to index

if (valid_move?(board, index) == true)  #if index is valid
  execute move(board, index, character = "X") #make the move for index
  display_board(board) #show the board
else   #else
  #ask for input again until you get a valid input
end #end
