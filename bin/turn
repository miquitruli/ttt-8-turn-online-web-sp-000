#!/usr/bin/env ruby
require_relative '../lib/turn.rb'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Welcome to Tic Tac Toe!"
display_board(board)

turn(board) #ask for input #convert input to index
converted_input = index
 
if (valid_move?(board, index) == true)  #if index is valid
  execute move(board, index, character = "X") #make the move for index
  display_board(board) #show the board
else   #else
  counter = 0
  until counter ==  #ask for input again until you get a valid input
end #end
